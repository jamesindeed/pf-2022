import React from 'react';

export default function SectionHeader({ title }) {
  return (
    <h6 className='uppercase font-semibold font-[Syncopate] text-[#626262] mb-[50px]'>
      {title}
    </h6>
  );
}
